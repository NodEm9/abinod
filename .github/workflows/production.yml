name: Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-${{ github.ref }}
  cancel-in-progress: true

jobs:
  production-check:
    name: Production readiness
    runs-on: ubuntu-latest
    timeout-minutes: 10

    env:
      NODE_ENV: production
      PORT: 3000

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check JavaScript syntax
        run: |
          node --check server.js
          node --check assets/site.js
          node --check assets/contact-form.js
          node --check sw.js

      - name: Start production server
        run: |
          npm start > server.log 2>&1 &
          echo $! > server.pid

      - name: Smoke test public routes
        run: |
          set -euo pipefail

          for route in \
            "/" \
            "/products/" \
            "/ecosystem/" \
            "/about/" \
            "/founder/" \
            "/mission/" \
            "/brand-essence/" \
            "/blog/" \
            "/blog/strong-foundations-create-stronger-systems/" \
            "/contact/" \
            "/docs/" \
            "/assets/site.js" \
            "/sw.js" \
            "/assets/og-abinod.png"
          do
            curl --fail --silent --show-error --retry 10 --retry-delay 1 "http://localhost:${PORT}${route}" > /dev/null
          done

      - name: Smoke test contact validation
        run: |
          status=$(curl --silent --output response.json --write-out "%{http_code}" \
            --request POST "http://localhost:${PORT}/api/contact" \
            --header "Content-Type: application/json" \
            --data '{"name":"A","email":"bad","topic":"General","message":"short"}')

          cat response.json

          if [ "$status" != "400" ]; then
            echo "Expected contact validation to return 400, got $status"
            exit 1
          fi

      - name: Show server logs
        if: always()
        run: cat server.log || true

      - name: Stop production server
        if: always()
        run: |
          if [ -f server.pid ]; then
            kill "$(cat server.pid)" || true
          fi
env: 
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
